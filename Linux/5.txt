5) Сделайте символическую ссылку на файл access.log в папке html.

ls -s ./html/access.log ./access #./html/access.log - существующий файл, для которого создаётся ссылка,
                                 #./access - имя символической ссылки
ls -l #Проверяю, создалась ли ссылка
